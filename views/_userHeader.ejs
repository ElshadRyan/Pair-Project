<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <a class="navbar-brand" href="#">MyApp</a>

    <div class="ms-auto d-flex gap-2">

        <a class="btn btn-outline-light btn-sm" href="/logout">Logout</a>

        <a class="btn btn-outline-light btn-sm" href="/<%= username %>/<%= id %>">Home</a>

        <a class="btn btn-outline-light btn-sm" href="/<%= username %>/<%= id %>/friends">Friends</a>

        <a class="btn btn-outline-light btn-sm" href="/<%= username %>/<%= id %>/add-post">Add Post</a>

        <a class="btn btn-outline-light btn-sm" href="/<%= username %>/<%= id %>/my-post">My Posts</a>

    </div>
</nav>

<div class="container mt-4">